import { AddHonorificsPipe } from './add-honorifics.pipe';

describe('AddHonorificsPipe', () => {
  it('create an instance', () => {
    const pipe = new AddHonorificsPipe();
    expect(pipe).toBeTruthy();
  });
});
